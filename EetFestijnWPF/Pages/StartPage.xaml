<Page x:Class="be.berghs.nils.EetFestijnWPF.Pages.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:lib="clr-namespace:be.berghs.nils.EetFestijnLib.viewModels;assembly=EetFestijnLib"
      
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=lib:MainViewModel}"
      Title="StartPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Start nieuwe sessie" Grid.Column="1" Grid.Row="1" Command="{Binding NewSessionCommand}"/>
        <Button Content="Sessie laden" Grid.Column="1" Grid.Row="2" Command="{Binding OpenSessionCommand}" />
    </Grid>
</Page>
